# Neon + Next.js CMS (skeleton)

- Next.js App Router (Node runtime) on Vercel
- Neon Postgres via @neondatabase/serverless

Routes:
- GET /api/health → checks DB connection

Env:
- DATABASE_URL
- JWT_SECRET
- NEXT_PUBLIC_SITE_NAME
